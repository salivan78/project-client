import React, {useEffect, useState} from "react";

const PerformChartPrint = () => {

    return (
        <div>
            <button onClick={() => alert('PRINT')}>Печать</button>
        </div>
    )
}

export default PerformChartPrint;